module.exports = {
  srcDir: "./src",
  distDir: "./dist",
  output: {
    css: true, // Only `.min.css` if false
    minCss: false,
  },
  blacklist: [], // Add filenames to skip (e.g., ['github.css'])
};
